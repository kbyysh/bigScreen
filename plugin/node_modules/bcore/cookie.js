Cube("/node_modules/bcore/cookie.js", [], function (module, exports, require, load, process, global) {

  function set(name, value, options) {
    options = options || {};
    if (value === null) {
      value = '';
      options.expires = -1;
    }
    var val = name + '=' + escape(value);
    var expires = options.expires;
    var path = options.path;
    var domain = options.domain;
    var secure = options.secure;
    if (expires) {
      var date;
      if (typeof expires == 'number') {
        date = new Date();
        date.setTime(date.getTime() + expires * 1000);
      } else if (expires.getTime) {
        date = expires;
      }
      if (date) expires = '; expires=' + date.toUTCString();else expires = '';
    }
    path = path ? 'path=' + path : '';
    domain = domain ? '; domain=' + domain : '';
    secure = secure ? '; secure' : '';
    document.cookie = [val, expires, path, domain, secure].join('; ');
  }
  function get(name) {
    var cookieValue = '';
    var cookies = document.cookie.split('; ');
    for (var i = 0, len = cookies.length; i < len; i++) {
      var cookie = cookies[i].replace(/^\s+|\s+$/g, '');
      cookie = cookie.split('=');
      if (cookie[0] == name) {
        cookieValue = cookie[1];
        break;
      }
    }
    return unescape(cookieValue);
  }
  ;
  exports.get = get;
  exports.set = set;return module.exports;});