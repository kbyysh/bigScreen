Cube("/node_modules/axios/lib/core/settle.js", ["/node_modules/axios/lib/core/createError.js"], function (module, exports, require, load, process, global) {

  var createError = require('/node_modules/axios/lib/core/createError.js');
  module.exports = function settle(resolve, reject, response) {
    var validateStatus = response.config.validateStatus;
    if (!response.status || !validateStatus || validateStatus(response.status)) {
      resolve(response);
    } else {
      reject(createError('Request failed with status code ' + response.status, response.config, null, response.request, response));
    }
  };return module.exports;});